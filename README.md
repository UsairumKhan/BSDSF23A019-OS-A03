## Version 7 – Pipes (v7)

This version adds **pipe (`|`) support** to myshell, while keeping 
previous features like I/O redirection (`<`, `>`, `>>`) and background execution (`&`).

### Features
- Connect commands using `|`
- Combine pipes with redirection
- Run entire pipelines in background with `&`

### Examples
